import React from 'react';
import { Typography } from '@mui/material';

const CardsContainer = () => {
	return (
		<Typography variant="h5" align="center" mt={2}>
			<strong>Game Score : </strong>0
		</Typography>
	);
};

export default CardsContainer;
